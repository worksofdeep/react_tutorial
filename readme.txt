Initial commit file for react tutorial

Setup:

1. Create public/index.html
2. Install live-server
    $ npm install live-server -g
    $ live-server -v
    $ live-server public   <--- folder path (index.html : default)